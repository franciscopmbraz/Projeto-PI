server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location ~* \.(js|css)$ {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}